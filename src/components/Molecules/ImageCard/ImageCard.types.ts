import { ImageProps } from '../../Atoms/Image/Image.types'
import { ImageDescriptionProps } from '../../Atoms/ImageDescription/ImageDescription.types'

export type ImageCardProps = {
  image?: ImageProps
  description?: ImageDescriptionProps
}

export type ImageCardType = {
  image?: ImageProps
  description?: ImageDescriptionProps
}
